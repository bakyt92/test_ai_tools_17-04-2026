// src/lib/supabase.ts
// Shared Supabase client used by both server components and API routes.

// createClient()
//   Initialises and exports a single Supabase client instance using
//   SUPABASE_URL and SUPABASE_ANON_KEY from environment variables.
//   Server-side only — never imported by client components.
